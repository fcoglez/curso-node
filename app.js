//con node, podemos ejecutar cualquier fichero en la terminal. Esto antes no era posible ya que solo se podia ejecutar en un navegador.

//para ejecutar ficheros, usamos el comando node nombreFichero.

function saludar(nombre){
    return `Hola ${nombre}`;
}

console.log(saludar("Faku"));